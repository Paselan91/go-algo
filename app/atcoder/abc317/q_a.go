package main
/*
import (
	"bufio"
	"fmt"
	"os"
	"strconv"
	"strings"
)

func main() {
	sc := bufio.NewScanner(os.Stdin)

	inputNHX := readStrings(sc)
	n, _ := strconv.Atoi(inputNHX[0])
	h, _ := strconv.Atoi(inputNHX[1])
	x, _ := strconv.Atoi(inputNHX[2])

	inputP := readStrings(sc)

	ans := -1
	for i := 0; i < n; i++ {
		p, _ := strconv.Atoi(inputP[i])
		if h+p >= x {
			ans = i + 1
			break
		}
	}

	fmt.Println(ans)
}

// func readString(sc *bufio.Scanner) string {
// 	sc.Scan()
// 	return sc.Text()
// }
func readStrings(sc *bufio.Scanner) []string {
	sc.Scan()
	return strings.Split(sc.Text(), " ")
}
*/
